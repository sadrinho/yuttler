// where bug reports and feedback go (the menu's feedback row and the load failed screen's "report a bug").
// one place so they can't drift apart
export const FEEDBACK_EMAIL = 'sadra.aliakbarpour@yale.edu'
